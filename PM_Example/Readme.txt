Power meter implementation plugin example

Required DMC references: Base.dll, PowerMeter.dll
Power meter plugin (PowerMeterPlugin.dll) loads power meters that are placed in Plugins folder, have name starting with "PM_" and implements IPowerMeterSettings interface.